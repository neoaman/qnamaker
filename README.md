# dash-plotly-template
Reusable template for Dash application with bootstrap components

## Installation
pip install dash pandas numpy holoviews[recommended] dask dash-bootstrap-components dash-extensions